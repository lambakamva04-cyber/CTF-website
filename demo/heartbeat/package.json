{
  "name": "ctf-demo-heartbeat",
  "version": "1.0.0",
  "private": true,
  "description": "Daily keep-alive so the Supabase free project never pauses under a live campaign",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "^5.9.0",
    "wrangler": "^4.131.0"
  }
}
